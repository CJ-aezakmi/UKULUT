# 📋 Как Вносить Изменения и Выпускать Обновления

## 🔄 Процесс работы с обновлениями

### 1️⃣ Вноси изменения в код как обычно

Редактируй любые файлы: TypeScript, Rust, стили и т.д.
Тестируй локально с помощью:
```powershell
npm run tauri dev
```

---

### 2️⃣ Когда готов к релизу - запусти скрипт!

Просто выполни одну команду:

```powershell
.\release.ps1 -Version "2.0.1" -Message "Исправлены баги с прокси и улучшен UI"
```

**Скрипт автоматически:**
- ✅ Обновит версию в `package.json`, `Cargo.toml`, `tauri.conf.json`
- ✅ Сделает git commit
- ✅ Отправит на GitHub
- ✅ Создаст тег - это запустит автоматическую сборку!

**Через 10 минут:**
- 📦 Новый установщик Windows появится в релизах
- 🔔 Все пользователи получат уведомление об обновлении!

---

## 🎯 Примеры использования

### Патч (исправление багов)
```powershell
.\release.ps1 -Version "2.0.1" -Message "Исправлен краш при запуске профиля с SOCKS5"
```

### Минорное обновление (новые функции)
```powershell
.\release.ps1 -Version "2.1.0" -Message "Добавлен импорт профилей из CSV, новые настройки canvas"
```

### Мажорное обновление (большие изменения)
```powershell
.\release.ps1 -Version "3.0.0" -Message "Полностью переработан движок, новый дизайн"
```

---

## 🛠️ Если нужно сделать вручную (без скрипта)

### Шаг 1: Обнови версии в 3 файлах

**package.json:**
```json
{
  "version": "2.0.1"
}
```

**src-tauri/Cargo.toml:**
```toml
[package]
version = "2.0.1"
```

**src-tauri/tauri.conf.json:**
```json
{
  "version": "2.0.1"
}
```

### Шаг 2: Закоммить и отправить

```powershell
git add .
git commit -m "Релиз v2.0.1 - Описание изменений"
git push origin main
```

### Шаг 3: Создать тег

```powershell
git tag v2.0.1
git push origin v2.0.1
```

🎉 Готово! Сборка запустится автоматически.

---

## 📊 Мониторинг релизов

### Проверить статус сборки:
https://github.com/CJ-aezakmi/UKULUT/actions

### Посмотреть все релизы:
https://github.com/CJ-aezakmi/UKULUT/releases

### Скачать последний установщик:
https://github.com/CJ-aezakmi/UKULUT/releases/latest

---

## 💡 Полезные советы

### Семантическое версионирование (SemVer)
- **2.0.1** → Патч (исправление багов)
- **2.1.0** → Минорная версия (новые функции, обратная совместимость)
- **3.0.0** → Мажорная версия (большие изменения, может быть несовместимость)

### Частота релизов
- **Hotfix (критический баг):** Сразу после исправления
- **Обычные обновления:** Раз в 1-2 недели
- **Большие фичи:** Когда готово + протестировано

### Тестирование перед релизом
```powershell
# Локальная сборка (проверить что всё компилируется)
npm run tauri build

# Установить и протестировать
# Установщик будет в: src-tauri\target\release\bundle\msi\
```

---

## 🔥 Что происходит после релиза?

1. **GitHub Actions** собирает установщик (~8-10 минут)
2. **Создаётся релиз** на странице releases
3. **Файлы загружаются:**
   - `Antic-Browser_2.0.1_x64_en-US.msi` - установщик
   - `latest.json` - манифест для автообновления
4. **Пользователи получают уведомление** при следующем запуске приложения

---

## 🛑 Откат релиза (если что-то пошло не так)

### Удалить последний тег:
```powershell
git tag -d v2.0.1
git push origin :refs/tags/v2.0.1
```

### Удалить релиз на GitHub:
Вручную на странице: https://github.com/CJ-aezakmi/UKULUT/releases

### Выпустить исправленную версию:
```powershell
.\release.ps1 -Version "2.0.2" -Message "Hotfix: исправлен критический баг из v2.0.1"
```

---

## ❓ FAQ

**Q: Можно ли редактировать код прямо на GitHub?**
A: Да, но после изменений нужно сделать `git pull` локально, потом запустить `release.ps1`

**Q: Сколько хранится история релизов?**
A: Все релизы хранятся вечно на GitHub (пока ты сам не удалишь)

**Q: Можно ли тестировать автообновление локально?**
A: Да, установи старую версию → выпусти новую → запусти приложение

**Q: Что если сборка упала с ошибкой?**
A: Смотри логи в Actions → исправь ошибку → выпусти патч-версию

---

## 🎓 Итого: Процесс от и до

```
1. Делаешь изменения в коде
          ↓
2. Тестируешь локально (npm run tauri dev)
          ↓
3. Запускаешь: .\release.ps1 -Version "2.X.X" -Message "..."
          ↓
4. Скрипт обновляет версии, коммитит, пушит, создаёт тег
          ↓
5. GitHub Actions автоматически собирает установщик
          ↓
6. Пользователи получают уведомление об обновлении!
```

**Просто. Быстро. Автоматически! 🚀**
