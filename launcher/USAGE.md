# 🎯 Как Использовать Лаунчер

## 📦 Что Такое Лаунчер?

**Antic Browser Launcher** - это отдельная программа, которая:
- ✅ Автоматически проверяет обновления основного приложения
- ✅ Скачивает и устанавливает обновления в фоне
- ✅ Проверяет все зависимости (Playwright, Chromium)
- ✅ Устанавливает недостающие компоненты
- ✅ Запускает основное приложение Antic Browser

---

## 🚀 Для Пользователя

### Первый Запуск

1. **Запусти Launcher** - двойной клик на `Antic Browser Launcher.exe`
2. **Подожди** - лаунчер автоматически:
   - Проверит наличие Antic Browser
   - Скачает и установит если нет
   - Проверит обновления
   - Установит Playwright/Chromium если нужно
3. **Готово!** - Antic Browser запустится автоматически

### Последующие Запуски

Просто запускай **Launcher** вместо основного приложения:
- Он проверит обновления
- Установит их если есть
- Запустит приложение

---

## 🛠️ Для Разработчика

### Установка и Тестирование

```powershell
# 1. Перейди в папку лаунчера
cd c:\Users\Asus\Desktop\AnticBrowser-Tauri\launcher

# 2. Установи зависимости
npm install

# 3. Запусти в режиме разработки
npm start
```

### Сборка Установщика Лаунчера

```powershell
cd launcher
npm run build
```

Установщик появится в `launcher\dist\`

### Что Происходит При Запуске?

```
1. Проверка установки Antic Browser (10%)
   ├─ Ищет: %LOCALAPPDATA%\Programs\Antic Browser\Antic Browser.exe
   └─ Если нет → скачивает с GitHub releases

2. Проверка обновлений (30%)
   ├─ Читает текущую версию из package.json
   ├─ Получает последнюю версию с GitHub API
   └─ Если новее → скачивает и устанавливает

3. Проверка Playwright/Chromium (60%)
   ├─ Проверяет node_modules/playwright
   ├─ Проверяет %LOCALAPPDATA%\ms-playwright\chromium-*
   └─ Если нет → пометка для установки при первом запуске

4. Проверка зависимостей (80%)
   ├─ Visual C++ Redistributable (опционально)
   └─ Другие системные библиотеки

5. Запуск приложения (95%)
   ├─ Запускает Antic Browser.exe
   └─ Закрывает лаунчер

6. Готово! (100%)
```

---

## 🔧 Настройка

### Изменение Репозитория

В `main.js` найди:

```javascript
const GITHUB_REPO = 'CJ-aezakmi/UKULUT';
```

Измени на свой репозиторий.

### Изменение Пути Установки

```javascript
const INSTALL_DIR = path.join(process.env.LOCALAPPDATA, 'Programs', APP_NAME);
```

### Добавление Дополнительных Проверок

В функции `checkDependencies()` можно добавить:

```javascript
// Проверка Node.js
const nodeVersion = process.version;
if (!semver.satisfies(nodeVersion, '>=18.0.0')) {
    throw new Error('Требуется Node.js 18+');
}

// Проверка свободного места
const freeSpace = await checkDiskSpace();
if (freeSpace < 500 * 1024 * 1024) { // 500 MB
    throw new Error('Недостаточно места на диске');
}
```

---

## 📊 Структура Проекта

```
launcher/
├── package.json          # Конфигурация npm и electron-builder
├── main.js              # Основная логика лаунчера
├── index.html           # UI лаунчера
├── icon.ico             # Иконка
└── README.md            # Документация
```

---

## 🎨 Кастомизация UI

### Изменить Градиент

В `index.html` найди:

```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

Замени цвета на свои.

### Изменить Эмодзи Логотип

```html
<div class="logo">🚀</div>
```

Замени 🚀 на любой другой эмодзи.

### Изменить Размеры Окна

В `main.js`:

```javascript
mainWindow = new BrowserWindow({
    width: 500,  // Ширина
    height: 400, // Высота
    ...
});
```

---

## 🐛 Решение Проблем

### "Не удалось установить"

**Причина:** Нет прав администратора или антивирус блокирует

**Решение:**
1. Запусти лаунчер от имени администратора
2. Добавь в исключения антивируса

### "Приложение не найдено после установки"

**Причина:** Установка не завершилась или неправильный путь

**Решение:**
1. Проверь путь в `INSTALL_DIR`
2. Увеличь задержку после установки в `main.js`:
   ```javascript
   await new Promise(resolve => setTimeout(resolve, 5000)); // 5 сек
   ```

### "Ошибка проверки обновлений"

**Причина:** Нет интернета или GitHub API недоступен

**Решение:** Лаунчер продолжит работу даже если проверка не удалась

---

## 🚀 Деплой Лаунчера

### Вариант 1: Отдельный Релиз

Собери лаунчер и загрузи в релизы:

```powershell
cd launcher
npm run build
```

Загрузи `dist/Antic Browser Launcher Setup.exe` в GitHub Release.

### Вариант 2: Вместе с Основным Приложением

Упакуй лаунчер в основной установщик как дополнительный компонент.

### Вариант 3: Автоматическая Сборка

Добавь в `.github/workflows/release.yml` еще один job для сборки лаунчера:

```yaml
launcher-build:
  runs-on: windows-latest
  steps:
    - uses: actions/checkout@v4
    - name: Build Launcher
      run: |
        cd launcher
        npm install
        npm run build
    - name: Upload Launcher
      uses: actions/upload-release-asset@v1
      with:
        asset_path: launcher/dist/Antic Browser Launcher Setup.exe
```

---

## 💡 Рекомендации

1. **Распространяй лаунчер вместо основного приложения**
   - Пользователи всегда получат последнюю версию
   - Автоматические обновления работают из коробки

2. **Добавь лаунчер в автозагрузку (опционально)**
   - Быстрый доступ к приложению
   - Проверка обновлений в фоне

3. **Логирование**
   - Все логи в консоли Electron (F12 в dev mode)
   - Добавь файловое логирование для production

---

**Вопросы?** Смотри основную документацию в `launcher/README.md`
