# AnticBrowser - Tauri Edition

Антидетект браузер переписанный с Python (Flet) на Tauri + Rust + React для лучшей производительности.

## Функции

- 🔐 **Управление профилями** - создание и хранение браузерных профилей с уникальными настройками
- 🌐 **Интеграция с прокси** - работа с SX.ORG и CyberYozh API для автоматического создания прокси
- 🎭 **Маскировка отпечатков** - подмена User-Agent, Screen Resolution, WebGL, CPU, RAM, Touch Screen
- 🍪 **Управление cookies** - автоматическое сохранение и загрузка cookies для каждого профиля
- 🚀 **Запуск через Playwright** - автоматизация запуска браузера с настройками профиля

## Технологии

### Frontend
- **React** - UI framework
- **TypeScript** - типизированный JavaScript
- **TailwindCSS** - utility-first CSS framework
- **Vite** - быстрый сборщик

### Backend
- **Rust** - системный язык программирования
- **Tauri** - фреймворк для desktop приложений
- **reqwest** - HTTP клиент
- **tokio** - асинхронный runtime

### Browser Automation
- **Playwright** - Node.js библиотека для автоматизации браузера
- **Chromium** - браузер на основе Chrome

## Установка

### Требования

- Node.js 18+ и npm
- Rust 1.75+
- Windows 10/11

### Шаги установки

1. Клонируйте репозиторий
```bash
cd AnticBrowser-Tauri
```

2. Установите Node.js зависимости
```bash
npm install
```

3. Установите Playwright браузеры
```bash
npx playwright install chromium
```

4. Запустите приложение в dev режиме
```bash
npm run tauri dev
```

## Сборка для продакшена

```bash
npm run tauri build
```

Скомпилированное приложение будет в `src-tauri/target/release/`.

## Структура проекта

```
AnticBrowser-Tauri/
├── src/                          # React frontend
│   ├── pages/                    # Страницы приложения
│   │   ├── ProfilesPage.tsx      # Управление профилями
│   │   └── ProxiesPage.tsx       # Управление прокси
│   ├── utils/                    # Утилиты
│   │   ├── constants.ts          # Константы (разрешения, языки, таймзоны)
│   │   └── storage.ts            # Локальное хранилище (legacy)
│   ├── api.ts                    # API клиент для Tauri commands
│   ├── types.ts                  # TypeScript типы
│   └── App.tsx                   # Главный компонент
├── src-tauri/                    # Rust backend
│   ├── src/
│   │   ├── models.rs             # Структуры данных
│   │   ├── storage.rs            # Работа с файловым хранилищем
│   │   ├── proxy_api.rs          # API клиенты (SX.ORG, CyberYozh)
│   │   ├── proxy_checker.rs      # Проверка прокси + GeoIP
│   │   ├── commands.rs           # Tauri commands (IPC handlers)
│   │   └── lib.rs                # Инициализация приложения
│   └── Cargo.toml                # Rust зависимости
├── playwright-launcher.js        # Скрипт запуска браузера
└── package.json                  # Node.js зависимости
```

## Архитектура

### Data Flow

```
React UI (TypeScript)
    ↓ (Tauri invoke)
Tauri Commands (Rust)
    ↓
Storage / API Clients / Proxy Checker
    ↓
File System / HTTP APIs / External Services
```

### Хранилище данных

Все данные хранятся в `%APPDATA%\com.anticbrowser.dev\`:

- `config/*.json` - профили браузеров
- `proxies.json` - список прокси
- `api_keys.json` - API ключи для SX.ORG и CyberYozh
- `proxy_cache.json` - кэш проверок прокси
- `cookies/*.json` - cookies для каждого профиля

## API Интеграции

### SX.ORG API
- Создание прокси по странам/регионам/городам
- Автоматическая валидация ключа
- Динамическая загрузка списков стран/регионов/городов

### CyberYozh API
- Просмотр магазина прокси
- Управление собственными прокси
- Автоматический импорт прокси в приложение

## Использование

### Создание профиля

1. Откройте страницу **Профили**
2. Нажмите **+ Создать профиль**
3. Заполните параметры:
   - **Название профиля** - уникальное имя
   - **User Agent** - можно сгенерировать случайный (кнопка 🎲)
   - **Разрешение экрана** - выберите из списка
   - **Язык и часовой пояс** - настройки локализации
   - **Прокси** - выберите из добавленных (опционально)
   - **Стартовая страница** - URL для автоматического открытия
   - **CPU/RAM/Vendor** - параметры для маскировки fingerprint
   - **WebGL и Touch Screen** - дополнительные настройки
4. Нажмите **Сохранить**

### Запуск профиля

1. Найдите нужный профиль в списке
2. Нажмите **▶ Запустить**
3. Браузер откроется с настройками профиля
4. Cookies автоматически сохраняются каждые 30 секунд
5. Закройте браузер вручную когда закончите

### Управление прокси

#### Быстрое добавление
Вставьте прокси в поле "Быстрый ввод" в любом формате:
- `192.168.1.1:8080:username:password`
- `http://username:password@192.168.1.1:8080`
- `socks5://192.168.1.1:1080`
- `192.168.1.1:8080`

Поля ручного ввода заполнятся автоматически!

#### Ручное добавление
1. Заполните поля: IP адрес, Порт, Протокол (HTTP/SOCKS5), Логин, Пароль
2. Нажмите **➕ Добавить прокси**

#### Проверка прокси
1. Найдите прокси в списке
2. Нажмите **Проверить**
3. Результат покажет:
   - ✅ Работает: страна, город, латентность
   - ❌ Не работает
   - ⚪ Не проверен

#### API интеграции
- **SX.ORG Прокси** (фиолетовая кнопка) - откроет сайт sx.org
- **CyberYozh Прокси** (черная кнопка) - откроет app.cyberyozh.com
- **PSB Proxy** (ссылка внизу) - откроет proxy-sale.com

## Разработка

### Добавление новой команды Tauri

1. Добавьте функцию в `src-tauri/src/commands.rs`
2. Зарегистрируйте команду в `src-tauri/src/lib.rs` в `.invoke_handler()`
3. Добавьте TypeScript wrapper в `src/api.ts`
4. Используйте в React компонентах

### Добавление нового поля в Profile

1. Обновите `Profile` struct в `src-tauri/src/models.rs`
2. Обновите `Profile` interface в `src/types.ts`
3. Обновите форму в `src/pages/ProfilesPage.tsx`
4. Обновите скрипты в `playwright-launcher.js`

## Troubleshooting

### Браузер не запускается

**Проблема**: При нажатии "Запустить" ничего не происходит

**Решение**:
- Убедитесь что Node.js установлен: `node --version`
- Убедитесь что Playwright установлен: `npm list playwright`
- Установите браузеры: `npx playwright install chromium`

### Прокси не работает

**Проблема**: Профиль запускается без прокси

**Решение**:
- Проверьте формат прокси строки
- Убедитесь что прокси рабочий через вкладку "Проверка"
- Проверьте логи в консоли браузера

### API ключи не сохраняются

**Проблема**: После перезапуска приложения API ключи пропадают

**Решение**:
- Проверьте что директория `%APPDATA%\com.anticbrowser.dev\` доступна
- Проверьте права на запись в эту директорию
- Посмотрите логи Tauri в консоли разработчика

## Лицензия

MIT

## Автор

Переписано с Python + Flet на Tauri + Rust + React
