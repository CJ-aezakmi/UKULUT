# 🚀 Быстрый Старт - Деплой на GitHub

## ✅ Пошаговая Инструкция

### 1️⃣ Генерация Ключей Подписи Tauri

Открой PowerShell в директории проекта и выполни:

```powershell
npm run tauri signer generate -- -w $env:USERPROFILE\.tauri\anticbrowser.key
```

**ВАЖНО:** Сохрани вывод! Тебе понадобится:
- **Публичный ключ** (начинается с `dW50cnVzdGVk...`)
- Расположение **приватного ключа**

### 2️⃣ Обнови tauri.conf.json Своим Публичным Ключом

Открой `src-tauri/tauri.conf.json` и замени pubkey:

```json
"plugins": {
  "updater": {
    "pubkey": "ВСТАВЬ_СВОЙ_ПУБЛИЧНЫЙ_КЛЮЧ_СЮДА"
  }
}
```

### 3️⃣ Настрой GitHub Репозиторий

#### Подключись к GitHub:
```powershell
cd c:\Users\Asus\Desktop\AnticBrowser-Tauri
git remote add origin https://github.com/CJ-aezakmi/UKULUT.git
git branch -M main
```

#### Добавь GitHub Secrets:
Перейди: https://github.com/CJ-aezakmi/UKULUT/settings/secrets/actions

Нажми **New repository secret** и добавь:

**Секрет 1:** `TAURI_SIGNING_PRIVATE_KEY`
- Значение: Открой файл `%USERPROFILE%\.tauri\anticbrowser.key` и вставь всё содержимое

**Секрет 2:** `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`
- Значение: (оставь пустым если не задавал пароль, или введи свой пароль)

### 4️⃣ Залей в GitHub

```powershell
git add .
git commit -m "Первый релиз v2.0.0 - Antic Browser с автообновлением"
git push -u origin main
```

### 5️⃣ Создай Тег Релиза

```powershell
git tag v2.0.0
git push origin v2.0.0
```

🎉 **Готово!** GitHub Actions автоматически:
- Соберёт Windows установщик (.msi)
- Создаст GitHub Release
- Загрузит установщик и манифест обновлений
- Подпишет всё твоим приватным ключом

### 6️⃣ Следи за Прогрессом Сборки

Смотри сборку здесь:
https://github.com/CJ-aezakmi/UKULUT/actions

Сборка займёт ~5-10 минут. Когда закончится, релиз будет тут:
https://github.com/CJ-aezakmi/UKULUT/releases/tag/v2.0.0

---

## 📦 Что Будет в Релизе

После успешной сборки пользователи смогут скачать:

- `Antic-Browser_2.0.0_x64_en-US.msi` - Windows установщик
- Приложение будет автоматически обновляться для будущих версий!

---

## 🔄 Для Будущих Обновлений

Когда захочешь выпустить v2.0.1:

1. Обнови версии в этих файлах:
   - `package.json`
   - `src-tauri/Cargo.toml`
   - `src-tauri/tauri.conf.json`

2. Закоммить и запушь:
   ```powershell
   git add .
   git commit -m "Релиз v2.0.1 - [опиши изменения]"
   git push
   git tag v2.0.1
   git push origin v2.0.1
   ```

3. Пользователи с v2.0.0 получат уведомление об обновлении!

---

## ❓ Решение Проблем

### "Permission denied" при push
Нужно авторизоваться в GitHub. Выполни:
```powershell
git config --global user.name "CJ-aezakmi"
git config --global user.email "твой-email@example.com"
```

Потом используй Personal Access Token для авторизации.

### Сборка падает на GitHub Actions
Проверь логи в разделе Actions. Частые проблемы:
- Ключи подписи не настроены правильно
- Отсутствуют зависимости (обычно устанавливаются автоматически)

### Нужна помощь?
Смотри `DEPLOYMENT.md` для подробного руководства.
